/*
Package errors provides the error interface used by the Blacksmith SDK and by the
adapters. It is also the recommended way to handle error in your application.

To avoid import cycles, this package should not import any other Blacksmith packages.
*/
package errors
